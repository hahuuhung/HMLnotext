# Danh sách công việc (Task List) - Advanced Node Catalog Upgrade

- `[ ]` **Task 1**: Cấu trúc lại các Custom Node components- [x] Cập nhật giao diện Node trong `CustomNodes.tsx` thay đổi thuộc tính nhãn, icon, và màu sắc theo `subtype`.
- [x] **Task 2**: Cập nhật hàm khởi tạo, kéo thả, và chọn nhanh node trong `src/App.tsx` để hỗ trợ thiết lập `subtype` mặc định cho các nhóm.
- [x] **Task 3**: Cập nhật giao diện thuộc tính Inspector trong `src/App.tsx` hỗ trợ chọn Sub-type và nhập các tham số tùy chọn con tương ứng.
- [x] **Task 4**: Cập nhật mô phỏng simulator `runWorkflow` sinh logs tùy biến theo từng Sub-type được cấu hình.
- [/] **Task 5**: Lint, build dự án.
- [ ] **Task 6**: Lưu tài liệu thiết kế (Mockup, Task, Plan, Walkthrough) vào thư mục `doc/`.
- [ ] **Task 7**: Push các thay đổi lên GitHub (`HMLnotext`).
