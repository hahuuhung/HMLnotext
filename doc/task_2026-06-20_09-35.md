# Nhiệm vụ phát triển: Xây dựng Dashboard Tools & Trợ lý sinh Prompt AI

- `[x]` **Bước 1: Cấu hình state lưu trữ và các công cụ mặc định (Default Tools State)**
  - [x] Thêm state `tools` và `selectedToolId` trong [App.tsx](file:///d:/AntiGravity/HML/src/App.tsx).
  - [x] Tạo danh sách 5 công cụ mặc định: Google Search, AI Scriptwriter, Stable Diffusion Visual, TTS Voiceover, Social Webhook.

- `[x]` **Bước 2: Cập nhật Chế độ xem & Thêm nút trên Header**
  - [x] Thay đổi kiểu dữ liệu `editorMode` thành `'workflow' | 'kdenlive' | 'tools'`.
  - [x] Thêm nút **"Quản lý Tools"** trong Header Mode Toggle.

- `[x]` **Bước 3: Xây dựng Giao diện Dashboard quản lý Tools**
  - [x] Tạo giao diện hai cột hiển thị khi `editorMode === 'tools'`.
  - [x] Cột bên trái: Danh sách thẻ công cụ (hoạt ảnh active, trạng thái kích hoạt) và nút `+ Tạo Công Cụ Mới`.
  - [x] Cột bên phải: Form chỉnh sửa chi tiết tên, loại, mô tả, endpoint URL và các tham số đầu vào.

- `[x]` **Bước 4: Tích hợp Trợ lý Prompt AI (AI Prompt Helper)**
  - [x] Bổ sung phần Trợ lý sinh mô tả / Prompt AI bên dưới form cấu hình công cụ.
  - [x] Viết logic sinh mô tả có cấu trúc tự động (System Prompt và JSON schema) mô phỏng cuộc gọi trợ lý LLM.

- `[x]` **Bước 5: Tích hợp Playground chạy thử nghiệm công cụ**
  - [x] Thêm phần nhập tham số kiểm thử và nút **"Chạy thử Công cụ"**.
  - [x] Thiết kế màn hình console hiển thị kết quả JSON trả về giả lập khớp với loại công cụ.

- `[x]` **Bước 6: Thiết kế CSS cho Dashboard và các Component**
  - [x] Thêm các class CSS cho layout, thẻ tool, console playground và hiệu ứng phát sáng trong [index.css](file:///d:/AntiGravity/HML/src/index.css).

- `[x]` **Bước 7: Kiểm thử biên dịch và chạy ứng dụng**
  - [x] Chạy `npm run build` để kiểm tra lỗi TypeScript.
  - [x] Sao lưu các tài liệu liên quan vào thư mục `doc/` với mốc thời gian mới.
