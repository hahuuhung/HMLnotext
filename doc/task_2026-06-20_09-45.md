# Nhiệm vụ phát triển: Resizable Layout, Shotcut NLE chuyên sâu & Google Flow Agent Dashboard

- `[x]` **Bước 1: Khai báo các state chiều rộng, chiều cao resizer và trạng thái Tool F&B Visual**
  - [x] Thêm state cho: `bottomPanelHeight`, `shotcutLeftWidth`, `shotcutRightWidth`, `shotcutTimelineHeight`.
  - [x] Thêm state cho F&B Seafood Sauce Visual Generator: `fbBrandName`, `fbBenefit`, `fbStyle`, `fbPhoto`, `fbStatus`, `fbGeneratedImages`.

- `[x]` **Bước 2: Viết các hàm kéo thả co dãn kích thước (VS Code Resizing handlers)**
  - [x] Viết hàm `startResizingBottom`, `startResizingShotcutLeft`, `startResizingShotcutRight`, `startResizingShotcutTimeline` sử dụng `mousemove` và `mouseup` toàn cục.

- `[x]` **Bước 3: Tích hợp nút chế độ xem thứ tư trên Header**
  - [x] Cập nhật `editorMode` kiểu dữ liệu thành `'workflow' | 'kdenlive' | 'tools' | 'agent-flow'`.
  - [x] Thêm nút **"Điều phối Agent AI"** trong Header.

- `[x]` **Bước 4: Thiết kế lại Giao diện Dựng Phim Shotcut chuyên nghiệp**
  - [x] Tích hợp Menu Bar và Toolbar với bộ nút chức năng (Open File, Export, etc.) theo Shotcut.
  - [x] Nâng cấp màn hình Preview Player với Timecode SMPTE, bộ phím điều hướng (Play/Pause, Loop, Fullscreen).
  - [x] Nâng cấp cột phải hiển thị Audio Peak Meter dọc nhảy dB động và danh sách Export Jobs.
  - [x] Nâng cấp Timeline dưới phân rõ các Track V1, V2, A1, S1 có nút Mute, Lock, Hide.

- `[x]` **Bước 5: Xây dựng Chế độ Điều phối AI Agent Google Flow & Tool F&B Visual**
  - [x] Thiết kế Light Theme (Breeze Light).
  - [x] Cột trái: Chatbot hội thoại AI Agent.
  - [x] Cột giữa: Sơ đồ Agent Flow và **Khu vực thiết kế chai nước chấm hải sản** (Upload chai nước chấm, chọn bối cảnh: Bàn ăn nhà hàng, Chụp từ trên xuống, Instagram, chạy tiến trình xử lý tách nền + đổ bóng và xuất ra 3 ảnh bối cảnh tương ứng cùng các nút tải tỷ lệ `1:1`, `4:5`, `9:16`).
  - [x] Cột phải: Tham số Agent cấu hình nhanh và Video Preview.

- `[x]` **Bước 6: Viết CSS hỗ trợ trong index.css**
  - [x] Bổ sung styles cho resizer ngang/dọc.
  - [x] Bổ sung styles cho các panel của Shotcut dark charcoal theme và dải dB meter.
  - [x] Bổ sung styles cho Agent Flow light theme và Tool F&B visual.

- `[x]` **Bước 7: Kiểm thử biên dịch, sao lưu và Git commit**
  - [x] Thực hiện `npm run build` để kiểm tra lỗi TypeScript.
  - [x] Sao lưu tài liệu vào thư mục `doc/` kèm timestamp.
  - [x] Thực hiện Git commit lưu trữ code.
