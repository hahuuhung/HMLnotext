# Task List: ViệtFlow

## A. Backend Infrastructure (Đã hoàn thành)
- `[x]` **Giai đoạn 1**: Khởi tạo Project & Cấu hình cơ bản (NestJS, Prisma).
- `[x]` **Giai đoạn 2**: Auth & Workspace Module (JWT, RBAC).
- `[x]` **Giai đoạn 3**: Project & Workflow CRUD.
- `[x]` **Giai đoạn 4**: Node Registry & Provider Abstraction.
- `[x]` **Giai đoạn 5**: Render Job Queue (BullMQ, Redis).
- `[x]` **Giai đoạn 6**: Bảo mật và Vận hành (Rate Limit, Audit Log, Asset Cleanup).
- `[x]` **Giai đoạn 7**: Triển khai (Docker, CI/CD, Sentry, Winston, Nginx).

---

## B. Lộ trình Triển khai Cơ bản (Product Roadmap)

### Phase 1: Prototype (Tập trung Cốt lõi)
- `[ ]` Canvas workflow kéo thả (React Flow Frontend).
- `[x]` Node registry cơ bản (Đã hoàn thiện phía Backend).
- `[ ]` Prompt-to-script (Gọi LLM API tạo kịch bản).
- `[ ]` Scene split (Chia cảnh).
- `[ ]` Generate image placeholder (Sinh ảnh nháp/đại diện).
- `[ ]` Render MP4 đơn giản bằng FFmpeg.

### Phase 2: MVP usable (Phiên bản khả dụng đầu tiên)
- `[ ]` Upload asset (Frontend -> S3).
- `[ ]` TTS (Chuyển văn bản thành giọng nói).
- `[ ]` Caption (Tạo phụ đề).
- `[ ]` Timeline preview (Xem trước timeline tại Frontend).
- `[x]` Render queue (Đã cấu hình Backend BullMQ).
- `[ ]` Template video ngắn.
- `[x]` Save/load workflow (Đã hoàn thiện CRUD Project API).

### Phase 3: Desktop + Local render
- `[ ]` Đóng gói desktop app (Electron / Tauri / .NET MAUI).
- `[ ]` Hỗ trợ local folder watch.
- `[ ]` Render bằng FFmpeg local.
- `[ ]` Sync project với cloud nếu cần.

### Phase 4: Production
- `[x]` Multi-user workspace (Đã hoàn thiện RBAC Backend).
- `[ ]` Billing/quota.
- `[ ]` Provider failover.
- `[ ]` Publish social.
- `[ ]` Advanced expression.
- `[ ]` Marketplace template/node.
