# Task List: Xây Dựng Backend Modules (Node.js/NestJS)

- `[x]` **Giai đoạn 1: Khởi tạo Project & Cấu hình cơ bản**
  - Khởi tạo NestJS project tại thư mục `server`.
  - Cấu hình Prisma ORM (Database schema cơ bản cho User, Workspace, Project).
  - Cấu hình Swagger API Documentation.
  
- `[x]` **Giai đoạn 2: Auth & Workspace Module**
  - Implement JWT Authentication (Login/Register).
  - API tạo/quản lý Workspace.

- `[x]` **Giai đoạn 3: Project & Workflow CRUD**
  - API tạo Project mới.
  - API lưu trữ JSON của biểu đồ (Nodes/Edges) và Versioning cơ bản.

- `[x]` **Giai đoạn 4: Node Registry & Provider Abstraction**
  - Định nghĩa danh sách các Node hợp lệ.
  - Cấu trúc thư mục Provider kết nối AI/Render.

- `[x]` **Giai đoạn 5: Render Job Queue**
  - Tích hợp BullMQ (Redis) để quản lý hàng đợi xuất video.

- `[x]` **Giai đoạn 6: Bảo mật và Vận hành**
  - Cập nhật Prisma Schema cho API Keys và Audit Log.
  - Mã hóa/Giải mã API keys.
  - Phân quyền RBAC cho Workspace/Project.
  - Rate limit theo user/workspace bằng `@nestjs/throttler`.
  - Cấu hình Job timeout, retry, cancellation trong BullMQ.
  - Content moderation placeholder cho AI-generated media.
  - Setup CronJob (Asset cleanup) với `@nestjs/schedule`.

- `[x]` **Giai đoạn 7: Triển khai (Deployment) - Nâng cao**
  - `[x]` **Component 1: Docker Packaging**
    - Tạo `.dockerignore`, `server/.dockerignore`.
    - Tạo `nginx.conf`.
    - Cập nhật `Dockerfile` (frontend & backend).
    - Cập nhật `docker-compose.yml`.
    - Tạo `.env.example`.
  - `[x]` **Component 2: CI/CD Pipeline**
    - Cập nhật `ci.yml`.
    - Cập nhật `cd.yml`.
  - `[x]` **Component 3: Giám sát & Ghi log**
    - Tạo `sentry-exception.filter.ts`.
    - Tạo `logging.interceptor.ts`.
    - Tạo `winston.config.ts`.
    - Tạo `health.controller.ts` và `health.module.ts`.
    - Cập nhật `main.ts` và `app.module.ts`.
