# Nhiệm vụ phát triển: Tích hợp MLT Engine (Lõi Shotcut) vào HML Video Editor

- `[x]` **Bước 1: Khởi tạo Dịch vụ MLT Generator (`mlt-generator.service.ts`)**
  - [x] Tạo file `server/src/render-queue/mlt-generator.service.ts`.
  - [x] Xây dựng class `MltGeneratorService` với phương thức `generateMltXml(scenes)`.
  - [x] Xử lý logic tạo cấu trúc XML chuẩn của Shotcut (`<mlt>`, `<profile>`, `<producer>`, `<playlist>`, `<tractor>`, `<multitrack>`, `<filter>`, `<transition>`).
  - [x] Ánh xạ các bộ lọc, hiệu ứng nền, hiệu ứng chuyển cảnh và tốc độ thành các tham số Frei0r và MLT filter tương ứng.

- `[x]` **Bước 2: Cập nhật Render Processor (`render.processor.ts`)**
  - [x] Nhúng `MltGeneratorService` vào `RenderProcessor`.
  - [x] Cập nhật hàm `handleRender` để tạo ra tệp `.mlt` tạm thời thay vì chạy FFMPEG ghép nối cứng.
  - [x] Thêm logic sử dụng `child_process.spawn` để thực thi lệnh `melt` (hoặc `qmelt`) gọi tới tệp `.mlt` vừa sinh ra và xuất ra tệp video `output.mp4`.
  - [x] Đảm bảo ghi log quá trình chạy của `melt` vào console (Stdout/Stderr) và cập nhật tiến trình Job (`job.progress`).

- `[x]` **Bước 3: Cấu hình Thực thi Engine Đa nền tảng**
  - [x] Tạo một hàm trợ giúp lấy đường dẫn của engine `melt.exe` dựa trên môi trường ứng dụng (từ thư mục `engine/` cục bộ hoặc biến môi trường).
  - [x] Bọc các xử lý đường dẫn bằng `path.join` để đảm bảo tương thích đa nền tảng.

- `[x]` **Bước 4: Kiểm thử, Xác minh & Tài liệu hóa**
  - [x] Chạy lệnh `npm run build` ở thư mục `server/` để kiểm tra lỗi TypeScript.
  - [x] Tạo bản cập nhật `walkthrough.md` tổng kết các luồng xử lý và cách engine MLT hoạt động.
  - [x] Lưu trữ các tệp kế hoạch, task và walkthrough vào thư mục `doc/` kèm timestamp.
  - [x] Commit mã nguồn.
