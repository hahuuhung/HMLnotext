# Nhiệm vụ phát triển: Thiết kế Giao diện Trực quan & Tối ưu No-code (Nhánh BigPjo)

- `[x]` **Bước 1: Nâng cấp Custom Nodes hiển thị tham số trực tiếp**
  - `[x]` Sửa [CustomNodes.tsx](file:///d:/AntiGravity/HML/src/components/CustomNodes.tsx) để đọc tham số từ Store toàn cục hoặc props `data` và hiển thị chi tiết (Prompt, File, URL, Model, Style, Voice, Platform) trực tiếp trên Node.
  - `[x]` Thêm styles phụ trợ cho các nhãn nhỏ, viền màu trạng thái động trên các Node.

- `[x]` **Bước 2: Thiết lập Bảng Cấu hình nhanh No-code trên Sidebar trái**
  - `[x]` Sửa [SidebarLeft.tsx](file:///d:/AntiGravity/HML/src/components/sidebar/SidebarLeft.tsx) / [App.tsx](file:///d:/AntiGravity/HML/src/App.tsx) để thêm biểu mẫu nhập dữ liệu Prompt/File/URL nhanh ngay bên dưới mỗi thẻ Template tương ứng.
  - `[x]` Tích hợp nút "Chạy Nhanh" (Quick Run) kích hoạt workflow tự động.

- `[x]` **Bước 3: Tích hợp đồng bộ trạng thái AI Agent hoạt động trên Console**
  - `[x]` Sửa [App.tsx](file:///d:/AntiGravity/HML/src/App.tsx) để đồng bộ trạng thái `agentStatuses` (`active/thinking`) phát sáng khi BFS Engine đang thực thi Node AI Script, Visual hoặc Audio TTS tương ứng.

- `[x]` **Bước 4: CSS Styles nâng cao cho Nodes & Sidebar**
  - `[x]` Sửa [index.css](file:///d:/AntiGravity/HML/src/index.css) để bổ sung CSS cho các trường nhập liệu nhanh ở Sidebar và các thẻ Node tự tài liệu hóa trên Canvas.

- `[x]` **Bước 5: Xác minh và Hoàn tất**
  - `[x]` Khởi động kiểm thử thủ công qua Electron dev server.
  - `[x]` Sao chép tài liệu `task.md`, `implementation_plan.md` và `walkthrough.md` vào thư mục `doc/` kèm mốc thời gian.
