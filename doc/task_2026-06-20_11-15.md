# Nhiệm vụ phát triển: Tích hợp Trợ lý Dựng phim AI (Gợi ý Hiệu ứng & Tốc độ)

- `[x]` **Bước 1: Khai báo các state phục vụ Trợ lý AI trong App.tsx**
  - [x] Thêm state `aiStyleGenre` (chọn phong cách video)
  - [x] Thêm state `customAiRequest` (nhập yêu cầu tùy chỉnh)
  - [x] Thêm state `isGeneratingSuggestions` (trạng thái chờ AI tính toán)
  - [x] Thêm state `aiSuggestions` (lưu trữ đề xuất hiện tại: bộ lọc, chuyển cảnh, nền, tốc độ, lý do)

- `[x]` **Bước 2: Viết hàm logic phân tích và đưa ra gợi ý hiệu ứng**
  - [x] Xây dựng hàm `generateAiSuggestions(sceneText, genre, customPrompt)` với luật heuristic/từ khóa thông minh (ví dụ: phát hiện "nhanh", "hành động" -> gợi ý Glitch + Fast speed; "chậm", "buồn" -> gợi ý Vintage + Slow speed).

- `[x]` **Bước 3: Viết các hàm áp dụng gợi ý của AI vào phân cảnh**
  - [x] Viết hàm `applyAllAiSuggestions()` để áp dụng đồng loạt cả 4 đề xuất.
  - [x] Viết các handler cho phép người dùng áp dụng từng gợi ý đơn lẻ.

- `[x]` **Bước 4: Thiết kế & tích hợp giao diện Trợ lý Gợi ý AI**
  - [x] Nhúng khối **Trợ lý Dựng phim AI** vào đầu thẻ `leftTab === 'filters'` trong Shotcut Layout.
  - [x] Bố trí dropdown chọn Thể loại, ô nhập Text Area cho yêu cầu tùy chỉnh, và nút ma thuật `🪄 Nhận Gợi ý Hiệu ứng AI` với hiệu ứng loading động.
  - [x] Hiển thị khu vực gợi ý kết quả (Glassmorphism container) gồm các tag hiệu ứng màu sắc rực rỡ và mô tả lý do từ AI.

- `[x]` **Bước 5: Thêm CSS bổ trợ trong index.css**
  - [x] Thêm class `.ai-assistant-container` với glassmorphism và viền sáng nhẹ.
  - [x] Thiết kế nút bấm `.ai-assistant-btn-magic` có gradient màu tím/xanh lấp lánh và hoạt ảnh pulse.
  - [x] Thêm styles cho các thẻ gợi ý (tags) hiển thị chuyên nghiệp.

- `[x]` **Bước 6: Kiểm thử biên dịch, chạy ứng dụng, sao lưu tài liệu và Git commit**
  - [x] Chạy `npm run build` để kiểm tra biên dịch dự án.
  - [x] Sao chép `task.md`, `implementation_plan.md`, và tạo `walkthrough.md` lưu trữ vào `doc/` kèm mốc thời gian.
  - [x] Thực hiện Git commit lưu phiên làm việc.
