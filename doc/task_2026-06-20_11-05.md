# Nhiệm vụ phát triển: Tích hợp Gợi ý Hiệu ứng Chuyển cảnh, Hiệu ứng Nền & Bộ chỉnh Tốc độ (Speed FX)

- `[x]` **Bước 1: Nâng cấp mô hình dữ liệu (Scene Interface)**
  - [x] Thêm thuộc tính `transition`, `bgFx` và `speed` vào giao diện `Scene` trong `App.tsx`.

- `[x]` **Bước 2: Phát triển các hàm xử lý sự kiện Thay đổi Thuộc tính**
  - [x] Viết hàm `handleSceneTransitionChange` để ghi nhận chuyển cảnh và xuất thông báo.
  - [x] Viết hàm `handleSceneBgFxChange` để ghi nhận hiệu ứng nền và xuất thông báo.
  - [x] Viết hàm `handleSceneSpeedChange` để ghi nhận tốc độ và xuất thông báo.

- `[x]` **Bước 3: Tích hợp các bộ lựa chọn (Selectors) vào Sidebar Inspector**
  - [x] Thêm dropdown cấu hình Chuyển cảnh, Hiệu ứng Nền, và Tốc độ phát bên dưới mục "Thiết lập Cảnh" ở Sidebar phải.

- `[x]` **Bước 4: Thiết kế lại tab "Bộ lọc" ở Sidebar trái của Shotcut Mode**
  - [x] Phân rã tab Bộ lọc thành 4 danh mục hiệu ứng: Bộ lọc màu, Hiệu ứng chuyển cảnh gợi ý, Hiệu ứng nền gợi ý, và Hiệu ứng tốc độ gợi ý.
  - [x] Tích hợp nút kích hoạt nhanh áp dụng trực tiếp cho phân cảnh đang được chọn.

- `[x]` **Bước 5: Thêm hiệu ứng hiển thị visual mô phỏng trong index.css**
  - [x] Bổ sung các lớp CSS mô phỏng bối cảnh nền hạt lấp lánh (particles), bokeh, khói smoke, gradient động, lưới retro, và vũ trụ.
  - [x] Bổ sung các lớp CSS mô phỏng tốc độ phát chậm (slow motion) và nhanh (high speed/vibration).

- `[x]` **Bước 6: Kiểm thử biên dịch và Commit**
  - [x] Thực hiện `npm run build` xác nhận biên dịch thành công.
  - [x] Git add và Git commit lưu trữ trên nhánh `BigPjo`.
